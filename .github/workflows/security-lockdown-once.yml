name: Security lockdown (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; security remediation is handled by the direct remediation workflow."
