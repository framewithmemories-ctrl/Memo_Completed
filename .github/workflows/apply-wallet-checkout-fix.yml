name: Apply wallet checkout integrity fix

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Apply wallet checkout fix
        shell: python
        run: |
          from pathlib import Path
          import re

          # This is intentionally a narrow, one-time migration of the existing
          # checkout implementation. It does not touch rewards, payment gateway,
          # product data, or existing orders.
          backend = Path('backend/server.py')
          b = backend.read_text()

          # 1) Normal/COD order requests must explicitly carry the checkout intent.
          if 'use_store_credit: bool = False' not in b:
              pattern = r'class OrderCreate\(BaseModel\):\n(.*?)(?=\nclass GiftQuizResponse\(BaseModel\):)'
              m = re.search(pattern, b, re.S)
              if not m:
                  raise SystemExit('OrderCreate model not found')
              block = m.group(0)
              marker = '    pickup_slot: Optional[str] = None'
              if marker not in block:
                  raise SystemExit('OrderCreate pickup_slot field not found')
              block = block.replace(marker, marker + '\n    use_store_credit: bool = False', 1)
              b = b[:m.start()] + block + b[m.end():]

          # 2) Replace only the legacy /orders endpoint. Razorpay /payments/create-order
          # already uses the server-authoritative pricing engine and remains unchanged.
          start_marker = '@api_router.post("/orders", response_model=Order)'
          end_marker = '\nclass PaymentVerifyRequest(BaseModel):'
          start = b.find(start_marker)
          end = b.find(end_marker, start)
          if start < 0 or end < 0:
              raise SystemExit('Normal order endpoint boundaries not found')

          new_route = '''@api_router.post("/orders", response_model=Order)
async def create_order(order: OrderCreate, current=Depends(get_current_user)):
    if order.user_id != current["id"]:
        raise HTTPException(status_code=403, detail="Not authorized to create an order for another account")
    if not order.items:
        raise HTTPException(status_code=400, detail="Order must contain at least one item")

    user = await db.users.find_one({"id": order.user_id})
    pricing = await _compute_order_pricing(
        order.items,
        order.delivery_type,
        order.use_store_credit,
        user,
    )

    order_dict = order.dict()
    order_dict["total_amount"] = pricing["final_amount"]
    order_dict["store_credit_applied"] = pricing["store_credit_applied"]
    order_dict["points_earned"] = pricing["points_earned"]
    order_obj = Order(**order_dict)

    # COD/pickup orders commit store credit at order creation, matching the
    # existing wallet-pay behavior. Razorpay orders commit it only after payment
    # verification in /payments/verify, so this path is not used for Razorpay.
    if pricing["store_credit_applied"] > 0:
        debit = await db.users.update_one(
            {"id": order.user_id, "store_credits": {"$gte": pricing["store_credit_applied"]}},
            {"$inc": {"store_credits": -pricing["store_credit_applied"]}},
        )
        if debit.matched_count != 1:
            raise HTTPException(status_code=409, detail="Store credit changed. Please refresh and try again.")

    try:
        await db.orders.insert_one(order_obj.dict())
    except Exception:
        if pricing["store_credit_applied"] > 0:
            await db.users.update_one(
                {"id": order.user_id},
                {"$inc": {"store_credits": pricing["store_credit_applied"]}},
            )
        raise

    if pricing["store_credit_applied"] > 0:
        updated_user = await db.users.find_one({"id": order.user_id})
        balance_after = float((updated_user or {}).get("store_credits", 0.0) or 0.0)
        await db.wallet_transactions.insert_one(
            WalletTransaction(
                user_id=order.user_id,
                type="debit",
                amount=pricing["store_credit_applied"],
                description=f"Store credit for order #{order_obj.id[:8]}",
                category="purchase",
                order_id=order_obj.id,
                balance_after=balance_after,
            ).dict()
        )

    return order_obj
'''
          b = b[:start] + new_route + b[end:]
          backend.write_text(b)

          # 3) Checkout UI must display and submit the same balance used by the backend.
          frontend = Path('frontend/src/components/EnhancedCheckoutPage.js')
          f = frontend.read_text()

          old_wallet_load = "axios.get(`${API}/users/${user.id}/wallet`).then((r) => setUserWallet({ balance: r.data.balance || 0 })).catch(() => {});"
          new_wallet_load = "axios.get(`${API}/users/${user.id}/wallet`).then((r) => setUserWallet({ balance: Number(r.data.store_credits || 0) })).catch(() => {});"
          if old_wallet_load in f:
              f = f.replace(old_wallet_load, new_wallet_load, 1)
          elif new_wallet_load not in f:
              raise SystemExit('Wallet balance loader not found')

          order_pos = f.find('const order = (await axios.post(`${API}/orders`, {')
          if order_pos < 0:
              raise SystemExit('Normal order payload not found')
          address_marker = '        delivery_address: address,'
          pos = f.find(address_marker, order_pos)
          if pos < 0:
              raise SystemExit('Normal order delivery address field not found')
          flag = '        use_store_credit: !!(useWalletBalance && user),'
          if flag not in f[pos:pos + 500]:
              f = f[:pos + len(address_marker)] + '\n' + flag + f[pos + len(address_marker):]

          old_wallet_pay = '''      if (useWalletBalance && user && totals.walletDiscount > 0) {
        await axios.post(`${API}/users/${user.id}/wallet/pay`, null, {
          params: { amount: totals.walletDiscount, order_id: order.id },
        }).catch(() => {});
      }

      finalizeOrder(order, totals);'''
          new_wallet_pay = '''      const serverTotals = {
        ...totals,
        walletDiscount: Number(order.store_credit_applied || 0),
        final: Number(order.total_amount || totals.final),
      };
      finalizeOrder(order, serverTotals);'''
          if old_wallet_pay in f:
              f = f.replace(old_wallet_pay, new_wallet_pay, 1)
          elif 'walletDiscount: Number(order.store_credit_applied || 0)' not in f:
              raise SystemExit('Legacy wallet payment block not found')

          # 4) Razorpay/mock checkout must use the pricing returned by the server.
          checkout_start = f.find('const handleRazorpay')
          submit_start = f.find('const handleSubmit', checkout_start)
          if checkout_start < 0 or submit_start < 0:
              raise SystemExit('Razorpay/submit boundaries not found')
          razor_section = f[checkout_start:submit_start]
          if 'const serverTotals = {' not in razor_section:
              anchor = "    if (info.mode !== 'production' || !info.key_id) {"
              pos = f.find(anchor, checkout_start)
              if pos < 0 or pos > submit_start:
                  raise SystemExit('Razorpay insertion point not found')
              pricing = '''    const serverTotals = {
      subtotal: Number(info.pricing?.subtotal || 0),
      delivery: Number(info.pricing?.delivery || 0),
      tax: Number(info.pricing?.tax || 0),
      walletDiscount: Number(info.pricing?.store_credit_applied || 0),
      final: Number(info.pricing?.final_amount || 0),
    };

'''
              f = f[:pos] + pricing + f[pos:]
          f = f.replace('await verifyAndFinalize(info.memories_order_id, totals, {});', 'await verifyAndFinalize(info.memories_order_id, serverTotals, {});', 1)
          f = f.replace('handler: (response) => verifyAndFinalize(info.memories_order_id, totals, response),', 'handler: (response) => verifyAndFinalize(info.memories_order_id, serverTotals, response),', 1)
          frontend.write_text(f)

          # Strong verification: fail before committing if the intended code is not present.
          checks = [
              ('OrderCreate use_store_credit', 'use_store_credit: bool = False', b),
              ('normal order server pricing', 'pricing = await _compute_order_pricing(', b),
              ('normal order authoritative total', 'order_dict["total_amount"] = pricing["final_amount"]', b),
              ('normal order stored credit', 'order_dict["store_credit_applied"] = pricing["store_credit_applied"]', b),
              ('frontend store credits', 'r.data.store_credits || 0', f),
              ('frontend order credit flag', 'use_store_credit: !!(useWalletBalance && user)', f),
              ('frontend server total', 'walletDiscount: Number(order.store_credit_applied || 0)', f),
              ('frontend payment pricing', 'info.pricing?.final_amount', f),
          ]
          missing = [name for name, needle, hay in checks if needle not in hay]
          if missing:
              raise SystemExit('Verification failed: ' + ', '.join(missing))

          print('Wallet checkout fix applied and verified.')

      - name: Validate Python syntax
        run: python -m py_compile backend/server.py

      - name: Commit application fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/server.py frontend/src/components/EnhancedCheckoutPage.js
          if git diff --cached --quiet; then
            echo "No application changes were needed."
            exit 0
          fi
          git commit -m "fix: align wallet checkout totals with store credits"
          git push
